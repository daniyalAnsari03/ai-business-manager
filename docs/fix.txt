Phase 1 implementation looks complete, but DO NOT start Phase 2 yet.

Your current TODO still shows:

[ ] Verify authenticated in-app rendering (needs user login)

Before declaring Phase 1 complete, finish the remaining verification requirements from the original Phase 1 prompt.

Do ONLY verification now. Do not modify unrelated features and do not start Phase 2.

1. Run typecheck and paste the actual terminal output.
2. Run lint and paste the actual terminal output.
3. Run production build and paste the actual terminal output.
4. Query/inspect Supabase and provide the actual RLS policy definitions for all 4 new tables:

   * social_posts
   * ad_campaigns
   * marketing_wallet
   * connected_accounts
     Do not just say RLS is enabled. Show the actual policies/definitions.
5. Using the running dev server, log in with a real authenticated user and manually verify:

   * Marketing tab opens successfully.
   * Posts this week = 0 / empty state as appropriate.
   * Ad spend = 0.
   * Sale from ads = 0 / empty as appropriate.
   * Activity empty state renders correctly.
   * Automation mode UI renders without errors.
   * No browser console errors.
6. Test the English/Roman Urdu language toggle on the Marketing page and record exactly what changes.
7. Open Settings → Marketing:

   * Instagram = Not connected
   * Facebook Page = Not connected
   * Google Ads = Not connected
   * WhatsApp number = Not connected
   * Connect buttons do not crash and show the intended coming-soon/disabled behavior.
8. Test Monthly ad budget cap:

   * enter a test amount
   * save it
   * verify it persists from Supabase / after refresh
   * then clean up the test value if appropriate.
9. Confirm no existing working feature was broken.
10. Provide the final report exactly against the original Phase 1 verification requirements, including:

* files created
* files modified + reason
* migrations applied
* actual RLS policy definitions
* raw typecheck/lint/build output
* authenticated UI verification
* English/Roman Urdu verification
* what is intentionally NOT functional in Phase 1

Do not report Phase 1 as complete until every verification item above has evidence.
