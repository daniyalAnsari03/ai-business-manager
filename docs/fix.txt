The whatsapp_inbound_events table has 0 rows even after I sent "YES" 
from my phone to the test number. Investigate why the webhook request 
never got recorded:

1. Check Vercel's Runtime Logs (or reproduce the exact check) around 
   the time I sent the message, specifically for the 
   /api/whatsapp/webhook route. Was there ANY incoming request logged 
   at all — even one that got rejected? Paste whatever you find.

2. Check the route's code flow: does persistInboundEvent() run BEFORE 
   or AFTER the HMAC signature verification (verifyHubSignature)? If 
   signature verification happens first and the event is REJECTED 
   there (e.g. returns 401/403 immediately), the event would never 
   reach persistInboundEvent() at all — meaning we'd see 0 rows even 
   if Meta genuinely sent the webhook. Check this order in the code and 
   tell me which happens first.

3. If signature verification runs first: temporarily add a log line 
   RIGHT BEFORE the signature check that logs "webhook request 
   received" regardless of outcome (so we can tell if Meta even 
   reached the server at all vs. rejected by our own signature check) 
   — then have me resend a test "YES" message and check again.

4. Separately, confirm in Meta's own dashboard: go to WhatsApp → 
   Webhooks section and check if there's a "Recent deliveries" or 
   similar log showing whether Meta actually attempted to call our 
   webhook URL when I sent the message, and what response code Meta 
   received back from our server (200, 401, timeout, etc.) — this 
   tells us definitively whether the request left Meta's side at all.

Paste whatever raw evidence you find at each step before concluding 
anything.